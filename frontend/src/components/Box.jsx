const Box = () => {
  return (
    <div
      style={{
        background:
          "linear-gradient(to right, #4facfe,rgba(182, 0, 254, 0.89))",
        padding: "3rem 1rem",
        textAlign: "left",
        color: "white",
      }}
    >
      <h2 className="mx-4 px-5 py-4 border rounded text-black bg-white fw-semibold fs-2">
        Login to Capture the moments and share it with the real world!
      </h2>
    </div>
  );
};

export default Box;
